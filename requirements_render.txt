# Hunt - Render deployment requirements
# Trimmed to what runs in a headless container. No desktop-automation libs
# (pyautogui, playwright, etc) — those only work on a real desktop with X/GUI.

# Core
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
python-multipart>=0.0.6
requests>=2.31.0
httpx>=0.27.0

# LLM clients
ollama>=0.6.0
openai>=1.30.0
langchain>=0.1.0
langchain-community>=0.0.30

# Voice (server-side STT)
openai-whisper>=20230314
soundfile>=0.12.0
numpy>=1.24.0

# Memory + embeddings
chromadb>=0.4.20
sentence-transformers>=2.2.2

# Advanced RAG: hybrid search (BM25 keyword + cosine vector) and document
# ingestion (PDF/text/markdown/code chunked into the same Chroma collection).
rank-bm25>=0.2.2
pypdf>=4.0.0

# Phase 3 — Gmail integration. Google's official client libs handle OAuth
# token exchange/refresh and provide a thin wrapper around the Gmail API.
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
google-api-python-client>=2.100.0

# MongoDB Atlas (you already use this)
pymongo>=4.6.0

# Optional: keep web search if you want it
duckduckgo-search>=4.0.0
